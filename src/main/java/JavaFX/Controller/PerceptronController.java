package JavaFX.Controller;

import NeuronNetwork.Perceptron;

public class PerceptronController {
    static Perceptron perceptron;

    public static void createPerceptron(String imageWidth) {
        //this.perceptron = new Perceptron();
        System.out.println("Perceptron creation !" + imageWidth);
    }
}
